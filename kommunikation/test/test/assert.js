let chai = require('chai');
let test = require('../test');
let assert = chai.assert;

let arr = [{
  foo: 2,
  bar: 4,
  var: 'hej',
  har: 'hej',
}];

describe('check value', function() {
  it('check obj with same value', function(){
    let group = test.groupBy(arr, )
  });
});
