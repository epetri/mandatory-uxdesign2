/*
Uppgift 3
Skriv tester för counter classen. Du ska använda den existerande counter instancen som är skapad i test.js.
Använd hooks för att återställa instancen med reset metoden efter varje test.

Du ska skriva minst två tester, där första testet ska testa att increase metoden funkar, och andra testat ska testa decrease metoden.
*/

function Counter() {
  this.count = 0;
}

Counter.prototype.increase = function() {
  this.count += 1;
};

Counter.prototype.decrease = function() {
  this.count -= 1;
};

Counter.prototype.getCount = function() {
  return this.count;
};

Counter.prototype.reset = function() {
  this.count = 0;
}

module.exports = Counter;
